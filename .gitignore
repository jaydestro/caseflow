node_modules
dist
.env
.env.*
!.env.example
*.log
.vite
coverage
.DS_Store

# TypeScript incremental build cache
*.tsbuildinfo

# Cosmos DB Agent Kit — installed globally via `npx skills add`, never vendored here
.agents/
